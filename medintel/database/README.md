# Database Scaffold

This directory is reserved for future database assets such as seed scripts, ERDs, or operational notes.

Current state:

- PostgreSQL is configured through environment variables.
- SQLAlchemy and Alembic are scaffolded under `backend/`.
- No tables, migrations, or seed data are included in this phase.

