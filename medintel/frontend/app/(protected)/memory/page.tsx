import { PagePlaceholder } from "@/components/shell/page-placeholder";

export default function MemoryPage() {
  return (
    <PagePlaceholder
      title="Memory"
      description="Clinical and operational memory capabilities will be introduced later. This page currently serves as a secure placeholder within the protected shell."
    />
  );
}

